{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
    {% if sform is defined %}
{% include "default/_form.html.twig" %}
    {% endif %}
<div><a href="{{ path('admin') }}">Admin</a></div>
    <div><a href="{{ path('app_login') }}">login</a></div>
    <div><a href="{{ path('app_register') }}">Register</a></div>
    <div><a href="{{ path('category_index') }}">category</a></div>
    <div><a href="{{ path('contentss_index') }}">content</a></div>


{% endblock %}
